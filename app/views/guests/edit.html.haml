%h2 Reserve More Seat(s)

%br
%br
- if @available_seats > 0
  = simple_form_for @guest, :url => event_guest_path do |guest|
    = guest.input :name
    = guest.input :num_of_seats, :collection => 1..@available_seats, :input_html => { value: 1 }
    = guest.button :submit, :method => :update
- else
  %h4 There are no longer available seats for this event

%br
= link_to "Back to Events", events_path
